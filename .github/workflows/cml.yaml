name: Model Training & SHAP Analysis

on: [push, pull_request]

jobs:
  train-and-evaluate:
    runs-on: [self-hosted, slurm, gpu, tillicum, cuda]

    env:
      CUDA_VISIBLE_DEVICES: "0"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify GPU access
        run: |
          echo "Checking GPU availability..."
          nvidia-smi
      # echo "CUDA version:"
      # nvcc --version

      # - name: Set up pixi
      #   uses: prefix-dev/setup-pixi@v0.9.4
      #   with:
      #     pixi-version: v0.66.0
      #     cache: true

      - name: Train model
        run: pixi run -e seasonality-model python testsmallmodel.py

      - name: Set up CML
        uses: iterative/setup-cml@v2

      - name: Report results
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "## Model metrics" > report.md
          cat metrics.txt >> report.md
          echo "![](./figures/beeswarm.png)" >> report.md
          echo "![](./figures/heatmap.png)" >> report.md
          cml comment create report.md



      # - name: Run SHAP analysis
      #   run: pixi run -e seasonality-model python shap_analysis.py

      # - name: Generate figures
      #   run: pixi run -e seasonality-model python generate_figures.py

      # - name: Report results to PR
      #   run: pixi run -e seasonality-model cml comment create --file figures/results.md

      # - name: Save artifacts
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: model-results
      #     path: |
      #       figures/
      #       models/
      #       *.pkl
      #       *.h5
      #     retention-days: 30
